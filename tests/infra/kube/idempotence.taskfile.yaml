version: "3"

tasks:
  default:
    desc: |
      ℹ️ Description:
        Test idempotence by re-running the deployment to ensure no changes occur
        This verifies that the infrastructure deployment is truly idempotent
    cmds:
      - cmd: echo "🔄 Starting idempotence testing phase..."
        silent: true
