---
apiVersion: v2
name: project
description: A Helm chart for project deployment with required dependencies
type: application
version: 9.2.0
appVersion: "6.0.0"

dependencies:
  - name: sealed-secrets
    version: "2.17.1"
    repository: "https://bitnami-labs.github.io/sealed-secrets"
    condition: sealed-secrets.enabled
