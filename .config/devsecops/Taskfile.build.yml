---
version: "3"

vars:
  TASK_DEVSECOPS_BUILD_ENABLED: '{{.TASK_DEVSECOPS_BUILD_ENABLED | default "true"}}'

tasks:
  default:
    desc: Run all generic build tasks
    status:
      - test "{{.TASK_DEVSECOPS_BUILD_ENABLED}}" = "false"
    cmds:
      - cmd: echo "🔄 Starting build phase"
        silent: true
      - task: :project:build
      - task: :helm:package
      - cmd: echo "🎉 Build phase completed successfully"
        silent: true
