---
version: "3"

vars:
  TASK_DEVSECOPS_BUILD_ENABLED: '{{.OVERRIDE_TASK_DEVSECOPS_BUILD_ENABLED | default "true"}}'

tasks:
  default:
    desc: Run all generic build tasks
    status:
      - test "{{.TASK_DEVSECOPS_BUILD_ENABLED}}" = "false"
    cmds:
      - task: :helm:build
